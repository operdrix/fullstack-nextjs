/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.pexels.com',
      'images.unsplash.com',
      'cdn.pixabay.com',
    ]
  }
}
module.exports = nextConfig
